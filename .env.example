# === Telegram Bridge ===

# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=

# Your Telegram Chat ID (message @userinfobot on Telegram to find this)
# Supports comma-separated values for multiple chats: "12345,-100987654"
TELEGRAM_CHAT_ID=

# Bridge mode: "dm" for direct messages (default), "group" for forum topics
# BRIDGE_MODE=dm

# === Email Bridge ===

# Your email address — only emails FROM this address are processed (required for email bridge)
# GMAIL_ALLOWED_SENDER=you@example.com

# Subject keyword to match (default: CLAUDE)
# GMAIL_KEYWORD=CLAUDE

# Poll interval in seconds (default: 60)
# GMAIL_POLL_INTERVAL=60

# Path to OAuth credentials directory (default: ~/.config/gmail-bridge)
# GMAIL_CONFIG_DIR=

# Path to email sessions file (default: ~/.claude-email-sessions.json)
# Per-subject session map — each unique subject line gets its own Claude session
# GMAIL_SESSIONS_FILE=

# === Shared ===

# Optional: absolute path to claude binary (auto-detected if omitted)
# CLAUDE_PATH=/opt/homebrew/bin/claude

# Path to sessions directory (default: ~/.claude-bridge-sessions)
# Stores per-topic session files and dedup state
# BRIDGE_SESSIONS_DIR=

# Legacy: path to single session file (default: ~/.claude-bridge-session)
# Existing sessions are auto-migrated to BRIDGE_SESSIONS_DIR
# BRIDGE_SESSION_FILE=
