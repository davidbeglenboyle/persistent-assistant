# === Telegram Bridge ===

# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=

# Your Telegram Chat ID (message @userinfobot on Telegram to find this)
TELEGRAM_CHAT_ID=

# === Email Bridge ===

# Your email address — only emails FROM this address are processed (required for email bridge)
# GMAIL_ALLOWED_SENDER=you@example.com

# Subject keyword to match (default: CLAUDE)
# GMAIL_KEYWORD=CLAUDE

# Poll interval in seconds (default: 60)
# GMAIL_POLL_INTERVAL=60

# Path to OAuth credentials directory (default: ~/.config/gmail-bridge)
# GMAIL_CONFIG_DIR=

# Path to email session state file (default: ~/.claude-email-session)
# GMAIL_SESSION_FILE=

# === Shared ===

# Optional: absolute path to claude binary (auto-detected if omitted)
# CLAUDE_PATH=/opt/homebrew/bin/claude

# Path to Telegram session state file (default: ~/.claude-bridge-session)
# Useful when running multiple bridge instances with separate sessions
# BRIDGE_SESSION_FILE=
